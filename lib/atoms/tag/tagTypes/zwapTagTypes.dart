/// The possible types for this tag component
enum ZwapTagType{
  clickable,
  static
}

/// The content types for this tag component
enum ZwapTagContentType{
  noIcon,
  leftIcon,
  rightIcon
}

/// The type of icon to render
enum ZwapIconType{
  widgetIcon,
  zwapIcon
}